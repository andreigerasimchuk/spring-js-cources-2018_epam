const list = () => {
  return Promise.resolve();
} 

const createItem = (data) => {
  return Promise.resolve();
}

const removeItem = (_id) => {
  return Promise.resolve();
}

const updateItem = (_id, change) => {
  return Promise.resolve();
}

const getItem = (_id) => {
  return Promise.resolve();
}

module.exports = {
  list,
  createItem,
  removeItem,
  updateItem,
  getItem,
}